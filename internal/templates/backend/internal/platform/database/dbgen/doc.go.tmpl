// Package dbgen is populated by `go tool sqlc generate`.
//
// This marker keeps the import path locally resolvable during the initial
// dependency tidy; sqlc owns every query/model implementation in this package.
package dbgen
