-- +goose Up
CREATE TABLE crm_deals (id UUID PRIMARY KEY, organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE, company_id UUID NOT NULL, pipeline_id UUID NOT NULL, stage_id UUID NOT NULL, name VARCHAR(200) NOT NULL, value_cents BIGINT NOT NULL DEFAULT 0 CHECK (value_cents >= 0), created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE (organization_id, id), FOREIGN KEY (organization_id, company_id) REFERENCES crm_companies(organization_id, id), FOREIGN KEY (organization_id, pipeline_id) REFERENCES crm_pipelines(organization_id, id), FOREIGN KEY (organization_id, stage_id) REFERENCES crm_stages(organization_id, id));
CREATE INDEX crm_deals_tenant_created_idx ON crm_deals (organization_id, created_at, id);
-- +goose Down
DROP TABLE crm_deals;
