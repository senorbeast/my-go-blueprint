-- +goose Up
CREATE TABLE crm_companies (id UUID PRIMARY KEY, organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE, name VARCHAR(200) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE (organization_id, id));
CREATE INDEX crm_companies_tenant_name_idx ON crm_companies (organization_id, name, id);
-- +goose Down
DROP TABLE crm_companies;
