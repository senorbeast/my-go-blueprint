-- +goose Up
CREATE TABLE crm_stages (id UUID PRIMARY KEY, organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE, pipeline_id UUID NOT NULL, name VARCHAR(200) NOT NULL, position INTEGER NOT NULL CHECK (position >= 0), created_at TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP, UNIQUE (organization_id, id), UNIQUE (pipeline_id, position), FOREIGN KEY (organization_id, pipeline_id) REFERENCES crm_pipelines(organization_id, id) ON DELETE CASCADE);
-- +goose Down
DROP TABLE crm_stages;
