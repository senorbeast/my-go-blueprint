-- +goose Up
CREATE TABLE membership_roles (organization_id uuid NOT NULL, user_id uuid NOT NULL, role_id uuid NOT NULL REFERENCES roles(id), PRIMARY KEY (organization_id, user_id, role_id), FOREIGN KEY (organization_id, user_id) REFERENCES memberships(organization_id, user_id));
-- +goose Down
DROP TABLE membership_roles;
