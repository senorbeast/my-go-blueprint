-- +goose Up
CREATE TABLE membership_roles (organization_id binary(16) NOT NULL, user_id binary(16) NOT NULL, role_id binary(16) NOT NULL, PRIMARY KEY (organization_id, user_id, role_id), FOREIGN KEY (organization_id, user_id) REFERENCES memberships(organization_id, user_id), FOREIGN KEY (role_id) REFERENCES roles(id));
-- +goose Down
DROP TABLE membership_roles;
