-- +goose Up
CREATE TABLE memberships(organization_id binary(16) NOT NULL,user_id binary(16) NOT NULL,PRIMARY KEY(organization_id,user_id),FOREIGN KEY(organization_id)REFERENCES organizations(id),FOREIGN KEY(user_id)REFERENCES users(id));
-- +goose Down
DROP TABLE memberships;
