# Implementation Progress

## Current Phase

Phase 9 — full generated stack implemented and verified; remaining production-depth adapters tracked below.

Status: in progress; upstream network import blocked, clean compatible implementation continuing.

## Completed

- Persisted the accepted master plan, clean feature-module architecture, and behavioral-testing plan.
- Created this implementation ledger before source changes.
- Attempted to add and fetch `https://github.com/melkeydev/go-blueprint.git` with approved access.
- Added shared typed configuration, stable feature dependency resolution, and manifest contracts.
- Verified shared specification tests with the working local Go 1.26 toolchain.
- Completed generator CLI foundation with create, dry-run, repeatable feature flags, manifest hashing, conflict-safe feature addition, verify, and doctor.
- Embedded backend/frontend template catalogs and wired them into the production CLI renderer.
- Generated and compiled real PostgreSQL and MySQL backend projects.
- Corrected SDK bootstrap, interceptor typing, Vitest configuration, and Playwright exclusion in the frontend template.
- Generated frontend now passes strict typecheck, Vitest, and production Vite build.
- Air/environment files and dialect-specific migrations/Compose files render correctly.
- Added interactive project creation alongside non-interactive flags.
- Added protected customization tracking so `add feature` preserves modified generated files while continuing additive generation.
- Added auth/session refresh-family, organization, RBAC, CMS, and CRM clean feature-module templates.
- Added strict PostgreSQL/MySQL migrations for RBAC, sessions, CMS, and CRM and normalized membership roles.
- Added API, worker, scheduler, migration, and idempotent seed commands.
- Added root generated README, Makefile, architecture, feature-authoring, and testing guides.
- Added generated behavioral tests for Huma health/OpenAPI, refresh replay family revocation, CMS workflow, CRM validation, and frontend CMS/CRM/auth behavior.
- Verified PostgreSQL and MySQL migrations up, reset to zero, and up again using isolated Docker containers.
- Verified minimal/demo seed behavior is idempotent on both databases with direct row-count assertions.
- Verified live generated API health and OpenAPI 3.1 endpoints.
- Cleaned all generated-project Docker containers, networks, and volumes after verification.
- Added deterministic greedy pairwise nightly scenario generation with a proof test covering every valid option pair.
- Added repository pull-request and nightly GitHub Actions workflows plus a generated-project CI workflow.
- Reworked the seed command to persist deterministic minimal/demo tenant, RBAC, CMS, and CRM records on both dialects.
- Added centralized Echo middleware for request IDs, recovery, security headers, body limits, credentialed CORS, timeouts, structured Zap access logs, and typed RFC 9457-style problems.
- Added logical route-module validation for prefixes, OpenAPI tags, authentication, and required permissions.
- Added a bootstrap-owned SQL database lifecycle for both generated dialects; startup now fails on an unreachable database and readiness probes the live connection instead of returning an unconditional success.
- Added a transactional auth refresh-session SQL Store adapter. It creates session/family/token records atomically, serializes refresh rotation, revokes replayed token families inside the transaction, and isolates PostgreSQL UUID versus MySQL `BINARY(16)` conversions in the adapter.
- Completed the generated credential-to-session slice: signup persists a bcrypt credential, creates an owner organization, and issues session cookies; login resolves the user's first organization and starts a new session. The generated CSRF middleware permits unauthenticated signup/login while rejecting unsafe cookie-authenticated requests without a matching token.

## Active Ownership

| Owner | Reserved scope | Status |
| --- | --- | --- |
| Primary agent | root files, `internal/spec`, shared contracts, integration, progress ledger | active |
| Generator agent | `cmd`, `internal/generator`, generator tests, `main.go`; no root/shared edits | handoff complete |
| Backend agent | `internal/templates/backend`; no root/shared edits | handoff complete |
| Frontend/testing agent | `internal/templates/frontend`, `internal/acceptance`; no root/shared edits | handoff complete |
| Auth/RBAC agent | new auth/organization backend feature and migration template paths | handoff complete |
| CMS agent | new CMS backend/frontend feature and migration template paths | handoff complete |
| CRM agent | new CRM backend/frontend feature and migration template paths | handoff complete |
| Auth transport agent | `internal/templates/backend/internal/platform/auth`, `internal/templates/backend/internal/platform/http`, auth HTTP adapter templates; no shared/root edits | active |
| Credentials agent | user auth-domain/application/repository templates and credential migration templates; no shared/root edits | active |
| Feature composition agent | organization/CMS repository and bootstrap-composition proposal templates; no shared/root edits | active |

## Decisions

- Build thin vertical slices and verify generated output after each slice.
- Use `.agents/skills/feature-slice-verification` as the mandatory completion workflow for future generated feature slices; a compile-only result remains partial evidence.
- Keep interfaces at real seams and generic CRUD/listing machinery internal.
- Select one database dialect per generated project.
- Use pairwise nightly coverage plus canonical pull-request scenarios.
- Retain the upstream URL and MIT attribution while implementing compatibly from the documented public CLI because source fetch is unavailable.
- Render assets through an embedded catalog, selecting one SQL dialect and including frontend assets only when requested.
- Preserve customized managed files during later feature additions and mark them in the manifest.
- Use `BINARY(16)` consistently for all MySQL IDs and foreign keys.

## Deviations

- Upstream history cannot yet be imported because this environment cannot connect to GitHub over HTTPS. The repository will record the upstream URL/revision status and can attach history when connectivity returns.
- The generated clean interfaces, schemas, commands, and representative behavior are implemented, but production sqlc repository Store adapters, SQL queue worker implementation, and SQL rate-limiter implementation remain follow-up slices. The credential, organization, and session adapters are currently handwritten `database/sql` adapters pending the cross-feature sqlc slice.
- The new refresh-session adapter is handwritten `database/sql` rather than sqlc-generated because the existing sqlc catalog does not yet declare auth session operations. Treat it as an interim concrete adapter: move its statements to the feature query catalog and replace its internal calls with generated queries when the cross-feature sqlc slice is implemented.
- The acceptance harness lifecycle, canonical/pairwise matrices, and GitHub Actions workflows are implemented. Playwright configuration and smoke specs are generated, but a live browser launch was not executed in this environment.

## Known Failures or Blockers

The canonical blocker register and its blocked todos are in [BLOCKERS.md](./BLOCKERS.md).

- GitHub fetch failed after escalation: `Failed to connect to github.com:443`.
- The system Go installation initially failed to resolve its standard library; subsequent verification succeeded. Keep the workspace-local `GOCACHE` and local Go fallback in `scripts/verify.ps1` for reproducibility.
- Docker Desktop is not running in this workspace (`//./pipe/docker_engine` is unavailable), so the organization-owner/CMS slice could not run its required PostgreSQL/MySQL migration replay, real-repository, composed HTTP, or live-stack/browser checks.

## Next Steps

1. Complete the remaining CRM find/update persistence as small verified slices, beginning with company updates, then run real-database adapter evidence.
2. With Docker available, execute the organization-owner/CMS slice against PostgreSQL and MySQL: migration up/reset/up, seed replay, real-repository and composed HTTP owner/non-member/cross-tenant tests, then a live authenticated browser flow. Mark it complete only after that evidence is recorded.
3. Compose real sqlc-backed SQL queue/worker and rolling-window limiter adapters after sqlc generation is available, then prove them against both databases.
4. Compose and behaviorally verify CRM routes with the real organization authorizer, including tenant/RBAC allow and denial behavior.
5. Retry upstream history import when GitHub connectivity is available.

## Verification Log

- Demo-ready generator foundation (partial): added a canonical 18-resolved-feature-graph acceptance contract; local PRD and dependency-ordered tracer-bullet issue documents; a generated shadcn-compatible frontend baseline (config, semantic tokens, reusable Button/Card/Badge primitives, capability-aware navigation, and real server logout); and documented bcrypt-backed development demo credentials (`owner@example.test` / `DemoPass123!`) for minimal/demo Auth seeds. The authenticated session context now exposes the caller's available organizations through a narrow listing seam without widening the authorization repository interface. Root `go test ./...` and `git diff --check` passed. A fresh complete PostgreSQL/demo fixture passed `go mod tidy && go tool sqlc generate && go test ./...`. Frontend installation/typecheck/build remains unverified: package installation retried against the registry with sandbox `EACCES` even after an approved dependency-download attempt. Platform-level super-admin persistence, organization invitation/role administration, CMS lifecycle/media/revisions, CRM reporting/import-export, and jobs/cron operational dashboards remain in the local tracer-bullet backlog rather than being claimed complete.

- Generated complete-app usability recovery: repaired the missing authenticated session-context route (`GET /api/v1/auth/me`), then added a usable sign-in/sign-up workspace shell that hydrates organization and permissions. CMS now supports CSRF-aware draft creation and publishing; CRM supports a company list/create/rename workflow. Generated PostgreSQL backend `go test ./...` and root `go test ./...` passed; targeted auth, CMS, and CRM Vitest tests plus the frontend typecheck and production builds passed. A later whole-suite Vitest invocation was interrupted by the desktop pnpm/Node runtime recreating native dependencies and retaining Node processes; this is recorded as an environment verification limitation, not a source failure. Remaining UX gap: visible sign-out currently clears client state rather than invoking the server logout endpoint.

- Full PostgreSQL development fixture: generated `full-stack-app` with auth, RBAC, jobs, cron, CMS, and CRM; corrected the CMS list-route query contract to avoid Huma's unsupported pointer query parameter; the generated CMS route-registration regression test passes. Docker PostgreSQL migrations reached version 35, the demo seed was applied, and live API health, Swagger docs, Vite frontend, and seeded database counts were verified.

- Parallel auth/credential/composition slice: root `scripts/verify.ps1` and `git diff --check` passed. A rendered complete PostgreSQL backend compile exposed a CMS PostgreSQL ID conversion type-shadowing error; the template was corrected and root verification rerun successfully. Full fixture rerun remains next.
- Complete PostgreSQL and MySQL auth/RBAC/CMS/CRM/jobs/cron fixtures: fresh generation plus `go mod tidy && go test ./...` passed for both dialects after the CMS ID-conversion fix.
- Credential-to-session slice: generated PostgreSQL and MySQL complete fixtures passed `go mod tidy && go test ./...`. Public generated HTTP tests verify signup issues session cookies without prior CSRF state and cookie-authenticated unsafe requests are rejected without CSRF; the application test verifies signup creates the owner organization and starts a session.
- Root `scripts/verify.ps1` now generates an isolated PostgreSQL auth fixture, runs `go mod tidy && go test ./...`, and passed with the credential and HTTP behavior tests included.
- Feature-completion plan and repository-local `feature-slice-verification` skill added. `scripts/verify.ps1` and `git diff --check` passed afterward. The prescribed Python skill initializer/validator could not run because no usable Python runtime is installed; the skill was created manually with the required frontmatter, UI metadata, and reference structure.
- Organization owner authorization/CMS composition slice (partial): signup-created organizations now persist an `owner` role, grant it `platform.admin`, and assign it to the creating membership transactionally when RBAC is selected. Bootstrap composes the organization-backed authorizer and registers organization and CMS route modules; it permits `platform.admin` and rejects missing permissions or an organization mismatch. Generated public tests cover owner CMS allowance, unprivileged-member denial, cross-tenant denial, and owner-grant creation. Fresh `auth+rbac+cms` PostgreSQL and MySQL backend fixtures each passed `go mod tidy && go test ./...`; `scripts/verify.ps1` and `git diff --check` passed. Docker was unavailable, so persistence migration/replay, real-repository/composed HTTP, and live browser evidence remain required before completion.
- SQL query-catalog slice (partial): added dialect-configured sqlc catalogs for auth session/refresh operations and organization/RBAC operations, plus an application-level session-start behavior test covering linked session/family/token persistence and issued principal. `scripts/verify.ps1`, fresh PostgreSQL and MySQL `auth+rbac+cms` fixtures, a fresh MySQL complete/demo fixture, and `git diff --check` passed. The CLI does not run `sqlc generate` and `sqlc` is unavailable in this environment, so the generated query packages cannot yet replace the handwritten stores; real-database and live-stack evidence remains blocked by Docker.
- SQL jobs/worker behavior slice (partial): added a jobs query catalog, feature-gated jobs rendering, and a public worker/store-boundary test that proves idempotency coalescing, retry-to-dead-letter at the maximum attempt count, and expired-lease recovery by a replacement worker. `scripts/verify.ps1`, a fresh PostgreSQL `jobs+cron` fixture, a fresh MySQL complete/demo fixture, and `git diff --check` passed. Docker is unavailable and sqlc generation is not composed, so transactional database claims, migration/reset/seed replay, and actual restart recovery remain required before completion.
- Rolling-window limiter slice (partial): added an in-process implementation behind the platform limiter interface with public tests for allowance to the configured limit, overflow rejection and retry/reset timing, window reset, tenant isolation, and concurrent admission (100 calls admit exactly the configured limit). `scripts/verify.ps1`, a fresh PostgreSQL default/full fixture, a fresh MySQL complete/demo fixture, and `git diff --check` passed. Docker is unavailable and sqlc generation is not composed, so real shared-database atomic windows, migration/reset/seed replay, HTTP limit headers, and multi-instance behavior remain required before completion.
- CRM composition slice (partial): bootstrap now composes CRM SQL list persistence, CRM services, and CRM routes with the organization authorizer; route tenant IDs are bound into the principal before application authorization. Public CRM application tests cover owner access, unprivileged-member denial, and cross-tenant denial. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`, fresh MySQL complete/demo fixtures, and `git diff --check` passed. Docker is unavailable, so migration/reset/seed replay, composed HTTP with a real database, and a live browser flow remain required; CRM create/find persistence remains uncomposed while the concrete adapter currently serves all list resources.
- sqlc reproducibility slice: generated backends pin `github.com/sqlc-dev/sqlc v1.30.0` as a Go tool and run `go tool sqlc generate` before their test target; root generated verification delegates to that target and `scripts/verify.ps1` now generates sqlc output in its auth fixture. The renderer now supplies a correct schema path, feature-local query directories, a baseline health query for featureless projects, and valid MySQL CRM query syntax; a public renderer test enforces the pinned tool and sqlc-before-compile behavior. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+cms` and MySQL complete/demo projects (`go mod tidy && go tool sqlc generate && go test ./...`), `go test ./internal/templates`, and `git diff --check` passed. The initial tool fetch required approved network access; database and browser evidence remains a separate dependent-feature gate.
- Auth sqlc-adapter slice (partial): replaced the handwritten session/refresh store statements with generated dbgen query calls, including transaction-scoped create/get/mark/revoke operations; added a public application refresh-rotation behavior test and the minimal generated package marker needed before initial sqlc output exists. `scripts/verify.ps1`, fresh auth-only PostgreSQL and MySQL projects (`go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker remains unavailable, so real migration/repository, composed cookie/CSRF HTTP, and browser evidence is still required.
- Organization/RBAC sqlc-adapter slice (partial): replaced the handwritten store with generated dbgen calls for transactional organization/owner creation, membership/default-organization lookup, permission lookup, and role assignment, retaining dialect UUID translation inside the adapter. Public application tests cover owner `platform.admin` provisioning plus authorized and denied role-assignment delegation. `scripts/verify.ps1`, fresh PostgreSQL and MySQL `auth+rbac` projects, a fresh MySQL `auth+rbac+cms` project (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker is unavailable, leaving real migration/replay, Testcontainers adapter, composed HTTP, and browser gates outstanding.
- CMS sqlc-adapter slice (partial): replaced handwritten CMS store statements with dbgen calls, retaining transactions for post/tag creation, tenant-scoped post/category/tag reads, workflow saves, and PostgreSQL UUID/MySQL `BINARY(16)` conversion within the adapter. A public application test proves an authorized create retains tenant, author, and tags while an authorization denial does not reach persistence. `scripts/verify.ps1`, fresh PostgreSQL and MySQL `auth+rbac+cms` projects (`go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker remains unavailable, leaving migration rollback/replay and seed evidence, Testcontainers, composed HTTP, and live browser proof outstanding.
- Jobs sqlc-adapter slice (partial): added generated query operations and a SQL Store for enqueue/idempotency lookup, lease recovery, transactional claiming, completion, retry, and dead-letter transitions; the generated worker command now opens its configured database, composes this Store, and polls `RunOnce`. Existing public worker behavior covers idempotency, retry/dead-letter, and expired-lease recovery. `go test ./...`, `scripts/verify.ps1`, fresh PostgreSQL `jobs`, fresh MySQL `jobs+cron`/demo projects (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker is unavailable, leaving migration reset/seed replay, Testcontainers, and live durable worker/restart evidence outstanding.
- SQL rolling-window limiter slice (partial): added an always-rendered rate-limit sqlc catalog and atomic shared `SQLRollingWindow` adapter keyed by a SHA-256 scope/subject/operation value; bootstrap composes it from the database handle and the adapter safely falls back in-memory when no database is supplied. Public tests cover limit/overflow/reset, tenant isolation, concurrency, and fallback behavior. `scripts/verify.ps1`, `git diff --check`, fresh PostgreSQL independent and fresh MySQL complete/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`) passed. Docker remains unavailable, leaving migration/replay, Testcontainers multi-instance proof, composed HTTP headers, and browser evidence outstanding.
- CRM sqlc-adapter slice (partial): replaced handwritten CRM list query loops with generated dbgen calls for all six routed list resources, retaining dialect UUID/`BINARY(16)` conversion inside the adapter and deterministic tenant-scoped list queries. The existing public application test covers authorized list access plus unprivileged and cross-tenant denials. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL `auth+rbac+crm`/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker is unavailable, leaving migration/reset/seed replay, Testcontainers, composed HTTP, and browser evidence outstanding; CRM create/find paths remain intentionally uncomposed and un-routed.
- CRM create-company slice (partial): added tenant/RBAC-authorized company creation through a generated `CreateCRMCompany` query, CRM SQL Store, bootstrap ID/clock composition, and `POST /api/v1/organizations/{organizationID}/crm/companies`. A public application test covers allow, unprivileged denial, cross-tenant denial, and validation; the red compile state was observed before implementation. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL complete/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker is unavailable, so migration/replay, real-repository/composed HTTP, and browser evidence remain required before completion.
- CRM create-contact slice (partial): added tenant/RBAC-authorized contact creation through generated `CreateCRMContact` persistence, SQL Store conversion, bootstrap composition, and `POST /api/v1/organizations/{organizationID}/crm/contacts`. Public behavior covers allow, unprivileged denial, cross-tenant denial, and blank-name validation. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL `auth+rbac+crm`/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. Docker is unavailable, leaving migration/reset/seed replay, Testcontainers, composed HTTP, and live browser evidence outstanding.
- CRM create-pipeline slice (partial): added tenant/RBAC-authorized pipeline creation through `CreateCRMPipeline` sqlc persistence, dialect UUID/`BINARY(16)` conversion, and `POST /api/v1/organizations/{organizationID}/crm/pipelines`. Public behavior covers owner success, unprivileged denial, cross-tenant denial, and blank-name validation. `scripts/verify.ps1`, fresh PostgreSQL and MySQL `auth+rbac+crm` fixtures, a fresh MySQL complete/demo fixture (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. B-001 prevents migration/replay/seed, Testcontainers, real-repository HTTP, and browser verification.
- CRM create-stage slice (partial): added tenant/RBAC-authorized stage creation with a tenant-scoped existing-pipeline check, normalized names, nonnegative position validation, `CreateCRMStage`/`FindCRMPipeline` sqlc persistence, and `POST /api/v1/organizations/{organizationID}/crm/pipelines/{pipelineID}/stages`. Public behavior covers owner success, unprivileged and cross-tenant denial, blank-name/negative-position validation, and unknown pipelines. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL complete/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. B-001 still blocks real-database, composed-HTTP, and browser evidence.
- CRM create-deal slice (partial): added tenant/RBAC-authorized deal creation with tenant-scoped company/pipeline/stage reference validation, explicit stage/pipeline matching, `CreateCRMDeal`/find-reference sqlc persistence, and `POST /api/v1/organizations/{organizationID}/crm/deals`. Public behavior covers owner success, unprivileged/cross-tenant denial, and invalid or mismatched references. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL complete/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. B-001 blocks real-database, composed-HTTP, and browser evidence. The MySQL verification command unexpectedly created untracked `crm-deal-mysql/` at the workspace root despite an output-path request; it is preserved for inspection rather than deleted automatically.
- CRM create-activity slice (partial): added tenant/RBAC-authorized activity creation, tenant-scoped deal reference validation, `CreateCRMActivity`/`FindCRMDeal` sqlc persistence, and `POST /api/v1/organizations/{organizationID}/crm/activities`. Public behavior covers owner success, unprivileged/cross-tenant denial, foreign-deal rejection, blank kind, and missing occurrence time. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL complete/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. B-001 blocks migration/replay, Testcontainers, real composed HTTP, and browser checks.
- CRM get-company detail slice (partial): added tenant/RBAC-authorized company lookup with `crm.companies.view`, typed domain-to-platform not-found mapping, tenant-scoped `FindCRMCompany` persistence, and `GET /api/v1/organizations/{organizationID}/crm/companies/{companyID}`. Public behavior covers owner retrieval, unprivileged/cross-tenant denial, and unknown-company not-found. `scripts/verify.ps1`, fresh PostgreSQL and MySQL `auth+rbac+crm` fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. B-001 blocks migration/replay, Testcontainers, real composed HTTP, and browser evidence.
- CRM update-company slice (partial): added tenant/RBAC-authorized company rename through a tenant-scoped find followed by `UpdateCRMCompany` sqlc persistence and `PATCH /api/v1/organizations/{organizationID}/crm/companies/{companyID}`. Public behavior covers normalized owner update, unprivileged/cross-tenant denial, blank-name validation, and typed unknown-company not-found. `scripts/verify.ps1`, fresh PostgreSQL `auth+rbac+crm`/minimal and MySQL `auth+rbac+crm`/demo fixtures (each with `go mod tidy && go tool sqlc generate && go test ./...`), and `git diff --check` passed. The red-test attempt encountered a concurrent default Go build-cache access denial but the isolated-cache rerun passed; B-001 blocks real-database, composed HTTP, and browser evidence.

- Planning files created; source verification pending.
- Upstream fetch attempted with approved access and failed due to external network connectivity.
- `C:\Users\has\.local\opt\go\bin\go.exe test ./...` with workspace `GOCACHE`: passed for `internal/spec`.
- `scripts/verify.ps1`: failed during concurrent work because generator-owned imports used the wrong module path; frontend acceptance and shared spec packages passed.
- `scripts/verify.ps1`: passed after generator handoff for root, CLI, acceptance, generator, and specification packages.
- Generated PostgreSQL backend: `go mod tidy && go test ./...` passed.
- Generated MySQL backend: `go mod tidy && go test ./...` passed.
- Generated frontend: `pnpm typecheck`, `pnpm test`, and `pnpm build` passed.
- Manifest safety: `add feature` rejected a managed `backend/go.mod` changed by `go mod tidy`.
- Manifest customization behavior: unit tests confirm later feature additions preserve user-modified managed files and verify the updated manifest.
- Full generated MySQL backend: compile and behavior tests passed.
- Full generated PostgreSQL backend: compile and behavior tests passed.
- Full generated React frontend: strict typecheck, three Vitest feature tests, and production build passed.
- Live generated API: `/healthz` returned `ok`; `/openapi.json` returned OpenAPI 3.1 with operation ID `health`.
- PostgreSQL Docker: migrations `up -> reset -> up` passed through version 35; demo seed applied twice; counts `users=1`, `organizations=1`, `cms_posts=1`, `crm_companies=1`.
- MySQL Docker: migrations `up -> reset -> up` passed through version 35; demo seed applied twice; counts `users=1`, `organizations=1`, `cms_posts=1`, `crm_companies=1`.
- Docker cleanup: all test containers, networks, and volumes removed.
- Pairwise nightly matrix proof: `TestNightlyScenariosCoverEveryValidOptionPair` passed.
- Root and generated GitHub Actions workflows added for generator, dialect compile, frontend, pairwise contract, Docker migration, rollback, and idempotent seed checks.
- Generated middleware/logical-route stack compiled and its public behavior tests passed.
- Database lifecycle template slice: `scripts/verify.ps1` passed. Fresh PostgreSQL and MySQL auth/RBAC backend fixtures each passed `go mod tidy && go test ./...`; the PostgreSQL retry required approved checksum-database network access after the sandbox denied the initial lookup.
- Auth refresh-session SQL adapter: `scripts/verify.ps1` passed. Fresh generated PostgreSQL and MySQL auth/RBAC backend fixtures each passed `go mod tidy && go test ./...`. The first PostgreSQL compile exposed an ID-decoding helper type-shadowing error; it was corrected before the successful rerun.
- Final repository checks: `go test ./...`, `go vet ./...`, and `git diff --check` passed.
- Auth invalid-credential HTTP slice: a fresh auth-only PostgreSQL fixture first reproduced a `500` response that leaked the domain `invalid credentials` message. The auth route template now maps only `users/domain.ErrInvalidCredentials` to a safe `401` problem response; unexpected errors remain `500`. The generated public HTTP regression tests cover both outcomes. Fresh fixture `go mod tidy && go tool sqlc generate && go test ./internal/features/auth/adapters/http`, root `go test ./internal/templates/...`, root `go test ./...`, and `git diff --check` passed. The existing `full-stack-app` fixture must be regenerated and restarted before it receives this template fix.
- Generated `make demo` slice: added a portable generated `demo` target and Windows PowerShell runner that create backend environment defaults, run `go mod tidy` before sqlc generation, start Docker PostgreSQL, migrate, replay-safe demo seed, API, selected worker/scheduler, and Vite. API/frontend host, port, CORS, and proxy settings are environment-overridable. Renderer behavior tests prove the command/docs and bootstrap ordering; focused template test, root `go test ./...`, and `git diff --check` passed. A brand-new complete PostgreSQL/demo fixture outside OneDrive ran only `$env:HTTP_ADDRESS=':18083'; $env:VITE_PORT='5176'; $env:VITE_API_PROXY_TARGET='http://localhost:18083'; make demo`: database reached healthy, migrations reached version 35, demo seed applied, `/healthz` and Swagger returned 200, Vite returned 200, and demo login plus `/auth/me` returned 200.
- Demo workspace discoverability slice (partial verification): reproduced a complete demo sign-in in the live browser where `/auth/me` returned only `platform.admin`; the backend authorizer accepts that grant as a wildcard but the generated frontend hid CMS/CRM navigation, cards, CMS actions, and CRM sections because it required literal feature permissions. Added a shared frontend capability evaluator that preserves the backend wildcard semantics and applied it to the generated header, dashboard, CMS route controls, and CRM view/create/update controls. Public frontend regressions now model the real `platform.admin` demo session for the dashboard and CRM while retaining the no-permissions denial case. A fresh full PostgreSQL/demo fixture rendered the changed files and root `go test ./...` plus `git diff --check` passed. Frontend Vitest/typecheck/build and post-fix browser proof remain pending because Windows denied read/remove access inside the disposable fixture's `node_modules` during dependency setup; no generated application source was edited as a workaround.
- CSRF repeat-sign-in repair: reproduced the reported `403 csrf_failed` deterministically: a first login creates session/CSRF cookies; a second login without `X-CSRF-Token` is correctly rejected, but the prior generated frontend omitted the header. Credential requests now echo the readable CSRF cookie and a frontend regression asserts that header. Root generator tests passed with an isolated Go build cache. A fresh all-features PostgreSQL/demo fixture was generated, migrated to version 35, replay-seeded idempotently, and started with Docker PostgreSQL. HTTP proof: first login `200`, repeated login without the header `403`, repeated login with its CSRF header `200`. The corrected fresh frontend is available on port 5178 and its API on port 18086.
- Post-login feature-route recovery: browser reproduction on the prior fresh demo showed that the dashboard's `Open CMS` and `Open CRM` links performed full page navigations; feature pages then read an unhydrated client-only active-organization store and rendered their empty selection placeholders, despite authenticated API checks returning the seeded CMS post and CRM company. The frontend root route now mounts a server-authoritative session hydrator for every route, restoring active organization and capability state after direct navigation/reload. CMS gained a visible editorial workspace (summary cards, search/status filters, seeded post, draft creation, and publish action); CRM accepts the platform-admin wildcard consistently and its transport models now emit camel-case JSON, exposing seeded company and deal data. A new disposable all-feature fixture (`go-blueprint-demo-ready-18100`) was browser-proven at `/cms` (seeded `Welcome` draft with publish action) and `/crm` (seeded `Acme Demo`, plus `Website refresh` and `Support expansion` deals); browser console errors were empty. A second fresh rendered frontend fixture passed `pnpm test` (9 tests), `pnpm typecheck`, and `pnpm build`; root `go test ./...` and `git diff --check` were rerun after the changes. Advanced CRM operations (Kanban mutation, import/export, reports, and full entity editing) remain planned work and are not claimed complete.
